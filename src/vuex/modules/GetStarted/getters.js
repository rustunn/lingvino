export function steps(state) {
  return state.GetStarted.steps;
}

export function currentStep(state) {
  return state.GetStarted.currentStep;
}

export function level(state) {
  return state.GetStarted.level;
}

export function stories(state) {
  return state.GetStarted.stories;
}
